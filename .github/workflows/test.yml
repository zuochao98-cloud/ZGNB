name: Test

on:
  push:
    branches: [main, feat/**]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest
          pip install -e .

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short

      - name: Self-optimizer unit tests
        run: source .venv/bin/activate && python -m pytest tests/test_reflex_blacklist.py tests/test_scorer.py tests/test_break_signal.py -v
        continue-on-error: true  # 观察期

      - name: Self-optimizer integration + E2E
        run: source .venv/bin/activate && python -m pytest tests/test_self_optimizer_integration.py tests/test_self_optimizer_e2e.py -v --tb=short
        continue-on-error: true

  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install "ruff>=0.15.0"


      - name: Lint
        run: ruff check modules/ tests/ --select=F,E,W,UP --ignore=E501,F401,F403 --exit-non-zero-on-fix

      - name: Format check
        run: ruff format --check modules/ tests/

  type-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install mypy
          pip install -e .

      - name: Run mypy
        run: python -m mypy modules/ --ignore-missing-imports

  quality-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run SKILL.md quality gate
        run: |
          pip install -e .
          python corpus/quality_check.py SKILL.md --json
          echo "=== summary ==="
          python corpus/quality_check.py SKILL.md --strict || true

      - name: Run SKILL.md dual-axis review
        run: |
          pip install -e .
          python corpus/dual_axis_review.py SKILL.md --json || true
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}

  e2e-realdata:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # secrets 不能在 job-level if: 用（GitHub Actions 限制）
    # 测试本身有 pytest.mark.skipif 兜底，无 token 时整文件 skip
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest
          pip install -e .

      - name: Run real data regression (600519.SH × MACD/KDJ/RSI vs stk_factor)
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          TUSHARE_API_URL: ${{ secrets.TUSHARE_API_URL }}
        run: |
          pytest tests/test_indicators_realdata.py -v --tb=short

  pre-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache pre-commit
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: pre-commit-

      - name: Run pre-commit
        run: |
          pip install pre-commit
          pre-commit run --all-files --show-diff-on-failure
