[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zettaranc-skill"
version = "4.1.0"
description = "zettaranc（万千）的思维框架与量化工具"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.12"
authors = [
    {name = "zettaranc-skill contributors"}
]
keywords = ["trading", "quantitative", "tushare", "technical-indicators", "bili"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial :: Investment",
]

dependencies = [
    "tushare>=1.4.0",
    "python-dotenv>=1.0.0",
    "pandas>=3.0,<4",
    "requests>=2.28.0",
    "pyyaml>=6.0.0",
    "httpx>=0.24.0",
]

[project.optional-dependencies]
corpus = [
    "yt-dlp>=2024.1.0",
    "faster-whisper>=1.0.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "maturin>=1.5.0",
]

[project.scripts]
zt = "modules.cli:main"
zt-web = "api.main:start_web"
zt-monitor = "modules.monitor:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["modules*"]
exclude = ["tests*", "scripts*", "corpus*", "docs*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short"
markers = [
    "slow: 慢速端到端测试（如 self_optimizer 跑多轮），默认不在常规 CI 中跑",
    "realdata: 真实数据回归测试（需 TUSHARE_TOKEN + RUN_REALDATA=true），默认 skip",
]

[tool.ruff]
line-length = 120
target-version = "py312"
extend-exclude = ["data/", "logs/", "knowledge/"]

[tool.ruff.lint]
select = ["F", "E", "W", "UP"]
ignore = ["E501", "F401", "F403"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["F811"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.12"
namespace_packages = true
explicit_package_bases = true
ignore_missing_imports = true
warn_unused_ignores = true
warn_return_any = false
disallow_untyped_defs = false  # 渐进式，不强制
disallow_incomplete_defs = false
no_implicit_optional = true
check_untyped_defs = true
strict = false  # 起步非 strict
exclude = [
    "data/",
    "logs/",
    "knowledge/",
    "tests/",
]

[[tool.mypy.overrides]]
module = "tests.*"
check_untyped_defs = false

[tool.coverage.run]
source = ["modules"]
omit = [
    "modules/__init__.py",
    "tests/*",
    "scripts/*",
    "corpus/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]
show_missing = true
precision = 1
fail_under = 0

[tool.coverage.html]
directory = "htmlcov"

[tool.maturin]
name = "_core_compute"
python-source = "python"
module-name = "_core_compute._core_compute"
